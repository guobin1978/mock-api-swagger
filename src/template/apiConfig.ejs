const mock = require('./data.js')

module.exports = {
  urls: { // 生成api
    testManage: 'http://127.0.0.1:5010/testManage/api.json?group=webapi', // swagger的json地址
  },
  proxy: { // 代理设置
    '/testManage': { // /testManage/* 的代理
      host: '127.0.0.1',
      port: '5010',
      children: { // 地址细化代理
        '/_test': { // /testManage/_test/* 的代理
          host: '127.0.0.1' ,
          port: '5020',
          mock: true
        }
      }
    }
  }
}